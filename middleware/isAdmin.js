var mongoose = require('mongoose');
var User = mongoose.model('User');

module.exports = function isAdmin (req, res, next) {
    // find super admin

    // if there is not super admin, adminsignup
    
}